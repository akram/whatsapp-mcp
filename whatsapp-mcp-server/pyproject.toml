[project]
name = "whatsapp-mcp-server"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "llama-stack",
    "fastmcp",
    "fastapi",
    "uvicorn",
    "sse-starlette",
    "python-multipart",
]
